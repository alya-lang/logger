# Types and data structures for logger
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct LoggerConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return LoggerConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
