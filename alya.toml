[package]
name = "logger"
version = "0.1.0"
alya-version = "0.0.19"
entry = "src/lib.alya"
description = "Structured logging, leveled output, JSON format, and file rotation for Alya"
authors = ["Alya Language Contributors <https://github.com/alya-lang>"]
license = "MIT"
homepage = "https://github.com/alya-lang/alya"
repository = "https://github.com/alya-lang/logger"
keywords = ["alya", "alya-lang", "package", "logger", "logging", "structured-logging", "json-logs", "log-rotation"]

[dependencies]

# [lint]
# disabled_rules = []
# exclude = []

# [fmt]
# exclude = []

# [test]
# exclude = []

# [bench]
# exclude = []
